; Общие настройки — применяются и в разработке, и на продакшене

; Контейнеры стартуют в UTC — задаём свой пояс явно
date.timezone = Europe/Moscow

memory_limit = 256M
upload_max_filesize = 32M
post_max_size = 32M
